You don’t have time to “study everything.” You need a **repeatable template** that works for ANY question from that PDF. I’ll give you exactly that—no fluff.

---

#  HOW TO HANDLE ANY QUESTION (MASTER STRATEGY)

Almost every question in your paper is just this:

 **Data → Clean → Analyze → Visualize → Insight**

So in exam:

1. **Load data**
2. **Clean text**
3. **Apply analysis (sentiment / topic / hashtag / etc.)**
4. **Plot graph**
5. **Write 2–3 insights**

If you follow this structure, you won’t get stuck.

---

# ⚡ UNIVERSAL PYTHON CODE (WORKS FOR MOST QUESTIONS)

Use this for:

* Twitter analysis
* Sentiment
* Hashtags
* Topic modeling
* Engagement

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import re
from textblob import TextBlob
from collections import Counter
from sklearn.feature_extraction.text import CountVectorizer
from sklearn.decomposition import LatentDirichletAllocation

# LOAD DATA
df = pd.read_csv("data.csv")

# CLEAN TEXT
def clean_text(text):
    text = str(text)
    text = re.sub(r"http\S+", "", text)
    text = re.sub(r"@\w+", "", text)
    text = re.sub(r"#", "", text)
    text = re.sub(r"[^A-Za-z ]", "", text)
    return text.lower()

df['clean_text'] = df['text'].apply(clean_text)

# =========================
# SENTIMENT ANALYSIS
# =========================
def get_sentiment(text):
    return TextBlob(text).sentiment.polarity

df['sentiment'] = df['clean_text'].apply(get_sentiment)

df['sentiment_label'] = df['sentiment'].apply(
    lambda x: 'Positive' if x > 0 else ('Negative' if x < 0 else 'Neutral')
)

print(df['sentiment_label'].value_counts())

# GRAPH
df['sentiment_label'].value_counts().plot(kind='bar')
plt.title("Sentiment Analysis")
plt.show()

# =========================
# HASHTAG ANALYSIS
# =========================
hashtags = []
for text in df['text']:
    hashtags += re.findall(r"#(\w+)", str(text))

top_hashtags = Counter(hashtags).most_common(10)
print(top_hashtags)

# =========================
# TOPIC MODELING (LDA)
# =========================
vectorizer = CountVectorizer(stop_words='english')
X = vectorizer.fit_transform(df['clean_text'])

lda = LatentDirichletAllocation(n_components=3)
lda.fit(X)

for i, topic in enumerate(lda.components_):
    print(f"Topic {i}:")
    print([vectorizer.get_feature_names_out()[index] for index in topic.argsort()[-5:]])

# =========================
# ENGAGEMENT ANALYSIS
# =========================
if 'likes' in df.columns:
    print("Avg Likes:", df['likes'].mean())

if 'retweets' in df.columns:
    print("Avg Retweets:", df['retweets'].mean())
```

---

# ⚡ HOW TO MODIFY THIS FOR EACH QUESTION

## 1. Twitter Analysis & Visualization

* Use sentiment + hashtags + bar chart
* Write:

  * “Majority tweets are positive”
  * “Trending hashtag is #AI”

---

## 2. Content / Topic Modeling

* Use LDA part
* Write:

  * Topic 1: AI tools
  * Topic 2: Education
  * Topic 3: Jobs

---

## 3. Location Analysis

```python
df['location'].value_counts().head(10).plot(kind='bar')
plt.show()
```

---

## 4. Hashtag Analysis

Already done above → just print top hashtags

---

## 5. Sentiment Analysis

Already done → show bar graph

---

## 6. Negative Tweets Detection

```python
negative = df[df['sentiment_label'] == 'Negative']
print(negative.head())
```

---

## 7. Engagement Analysis

```python
df[['likes','retweets']].mean().plot(kind='bar')
plt.show()
```

---

## 8. Google Trends (simple)

```python
from pytrends.request import TrendReq
pytrends = TrendReq()

pytrends.build_payload(['ChatGPT','Gemini','Claude'])
data = pytrends.interest_over_time()

data.plot()
plt.show()
```

---

## 9. Network Analysis (IMPORTANT QUESTION)

```python
import networkx as nx

G = nx.Graph()

edges = [('A','B'),('A','C'),('B','D')]
G.add_edges_from(edges)

nx.draw(G, with_labels=True)
plt.show()

print("Centrality:", nx.degree_centrality(G))
```

---

# 🧠 WHAT TO WRITE IN THEORY (VERY IMPORTANT)

For ANY answer:

### 👉 Write this format:

**1. Objective:**
Analyze sentiment / hashtags / engagement

**2. Method:**

* Data collection
* Text preprocessing
* Applied NLP techniques

**3. Result:**

* Positive/Negative distribution
* Top hashtags
* Key topics

**4. Conclusion:**

* Public opinion is mostly positive
* Topic is trending due to X reason

---

# 🔥 FINAL EXAM HACK

If stuck:

👉 Write:

* Clean text
* Apply sentiment
* Plot graph
* Write 3 insights


